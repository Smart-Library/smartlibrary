<% breadcrumb @desk %>
<div class="col-lg">
  <div class="card">
    <div class="card-body">
      <h4 class="card-title"><%= @desk.name %></h4>
      <p class="card-text">Desk ID: <%= @desk.id %></p>
      <div class="text-center">
        <div class="alert alert-<%= desk_class(@desk) %>" role="alert"><%= desk_status(@desk) %></div>
      </div>
    </div>
  </div>
</div>
